# Local Instructions

## package.json'version

if you are editing any files of the packages, let's bump the package.json'version so when files are pushed, github action succeeds on publishing the new packages.
